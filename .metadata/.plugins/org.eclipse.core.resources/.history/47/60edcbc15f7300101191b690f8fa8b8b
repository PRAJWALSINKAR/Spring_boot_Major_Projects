<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>View Applications</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
<nav class="navbar navbar-light bg-light px-3">
    <a href="/dashboard" class="btn btn-sm btn-secondary">Back</a>
    <span class="navbar-brand mx-auto">IES Applications</span>
    <div class="d-flex">
        <a href="/dashboard" class="btn btn-light btn-sm me-2">Dashboard</a>
        <a href="/logout" class="btn btn-danger btn-sm">Logout</a>
    </div>
</nav>

<div class="container mt-4">
    <h3>Citizen Applications</h3>

    <table class="table table-bordered mt-3">
        <thead>
            <tr>
                <th>Case No</th>
                <th>Full Name</th>
                <th>Email</th>
                <th>SSN</th>
            </tr>
        </thead>
        <tbody>
            <tr th:each="citizen : ${citizens}">
                <td th:text="${citizen.caseNumber}">123</td>
                <td th:text="${citizen.fullName}">Name</td>
                <td th:text="${citizen.email}">Email</td>
                <td th:text="${citizen.ssn}">SSN</td>
            </tr>
        </tbody>
    </table>
</div>
</body>
</html>
