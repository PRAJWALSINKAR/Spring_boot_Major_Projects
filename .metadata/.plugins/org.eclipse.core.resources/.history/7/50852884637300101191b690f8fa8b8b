<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>View Applications</title>
    <link rel="stylesheet" th:href="@{/css/bootstrap.min.css}">
    <style>
        body {
            display: flex;
            height: 100vh;
            margin: 0;
            font-family: 'Segoe UI', sans-serif;
        }

        /* Sidebar styles */
        .sidebar {
            width: 200px;
            background-color: #343a40;
            color: white;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .sidebar img {
            width: 50px;
            height: 50px;
            margin-bottom: 10px;
        }

        .sidebar button {
            background: none;
            border: none;
            color: white;
            font-size: 24px;
            margin-bottom: 20px;
        }

        /* Main content area */
        .main-content {
            flex: 1;
            background-color: #f8f9fa;
        }

        .top-nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 30px;
            background-color: #343a40;
            color: white;
        }

        .top-nav .btn {
            margin-left: 10px;
        }

        .content-area {
            padding: 30px;
        }

        table {
            margin-top: 20px;
        }
    </style>
</head>
<body>

<!-- Sidebar with back and logo -->
<div class="sidebar">
    <button onclick="history.back()" title="Go Back">&#8592;</button>
    <img th:src="@{/images/myName-Logo.png}" alt="Logo">
    <h5>IES</h5>
</div>

<!-- Main Content -->
<div class="main-content">

    <!-- Top Navbar -->
    <div class="top-nav">
        <h4 class="m-0">View Applications</h4>
        <div>
            <a href="/dashboard" class="btn btn-light btn-sm">Dashboard</a>
            <a href="/logout" class="btn btn-danger btn-sm">Logout</a>
        </div>
    </div>

    <!-- Data Table Area -->
    <div class="content-area">
        <table class="table table-bordered table-striped">
            <thead class="table-dark">
                <tr>
                    <th>Sr No</th>
                    <th>Full Name</th>
                    <th>Email</th>
                    <th>SSN</th>
                    <th>Gender</th>
                    <th>DOB</th>
                </tr>
            </thead>
            <tbody>
                <tr th:each="citizen, iStat : ${citizens}">
                    <td th:text="${iStat.index + 1}"></td>
                    <td th:text="${citizen.fullName}"></td>
                    <td th:text="${citizen.email}"></td>
                    <td th:text="${citizen.ssn}"></td>
                    <td th:text="${citizen.gender}"></td>
                    <td th:text="${citizen.dob}"></td>
                </tr>
            </tbody>
        </table>
    </div>

</div>

</body>
</html>
