<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Eligibility Determination</title>
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
    <style>
        body { background-color: #f8f9fa; font-family: 'Segoe UI', sans-serif; }
        .navbar { background-color: #343a40 !important; }
        .navbar-brand img { height: 40px; width: 40px; }
        .ed-container {
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.04);
            padding: 32px;
            margin: 40px auto;
            max-width: 600px;
            text-align: center;
        }
        .headline { margin-bottom: 25px; }
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark px-3 py-2">
    <div class="d-flex align-items-center">
        <button onclick="history.back()" class="btn btn-outline-light me-3" title="Go Back">&#8592;</button>
        <a class="navbar-brand d-flex align-items-center" href="#">
            <img th:src="@{/images/myName-Logo.png}" alt="Logo" class="me-2" />
            <span>IES</span>
        </a>
    </div>
    <div class="ms-auto d-flex gap-2">
        <a href="/dashboard" class="btn btn-light btn-sm">Dashboard</a>
        <a href="/logout" class="btn btn-danger btn-sm">Logout</a>
    </div>
</nav>

<div class="ed-container">
    <h4 class="headline">Eligibility Determination</h4>
    <div class="mb-4">
    <label class="form-label"><strong>Case Number:</strong></label>
    <input type="text" name="caseNumber" th:value="${caseNumber}" class="form-control"
           placeholder="Enter or confirm case number" required>
</div>

    <div class="mb-4 text-secondary">
        We are now ready to check your eligibility for the selected plan and benefits.
    </div>
    <form th:action="@{/ed/startEligibility}" method="post">
        <input type="hidden" name="caseNumber" th:value="${caseNumber}" />
        <button type="submit" class="btn btn-primary btn-lg">Determine Eligibility</button>
    </form>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
