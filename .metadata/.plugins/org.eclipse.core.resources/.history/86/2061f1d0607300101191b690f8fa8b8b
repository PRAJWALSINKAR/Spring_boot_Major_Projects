<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>View Applications</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <!-- Header Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-3">
        <button onclick="history.back()" class="btn btn-outline-light me-2" title="Go Back">&#8592;</button>
        <a class="navbar-brand d-flex align-items-center" href="#">
            <img th:src="@{/images/myName-Logo.png}" alt="Logo" width="40" height="40" class="me-2"/>
            <span>IES</span>
        </a>
        <div class="ms-auto d-flex gap-2">
            <a href="/dashboard" class="btn btn-light btn-sm">Dashboard</a>
            <a href="/logout" class="btn btn-danger btn-sm">Logout</a>
        </div>
    </nav>

    <div class="container mt-4">
        <h3>Citizen Applications</h3>

        <table class="table table-bordered mt-3">
            <thead>
                <tr>
                    <th>Case No</th>
                    <th>Full Name</th>
                    <th>Email</th>
                    <th>Mobile Number</th>
                    <th>Gender</th>
                    <th>DOB</th>
                    <th>SSN</th>
                </tr>
            </thead>
           <tbody>
    <tr th:if="${#lists.isEmpty(citizens)}">
        <td colspan="7" class="text-center">No applications found</td>
    </tr>
    <tr th:each="citizen : ${citizens}">
        <td th:text="${citizen.caseNumber}">123</td>
        <td th:text="${citizen.fullName}">Name</td>
        <td th:text="${citizen.email}">Email</td>
        <td th:text="${citizen.mobileNumber}">Mobile</td>
        <td th:text="${citizen.gender}">Gender</td>
        <td th:text="${citizen.dob != null} ? ${#dates.format(citizen.dob, 'yyyy-MM-dd')} : 'N/A'">DOB</td>
        <td th:text="${citizen.ssn}">SSN</td>
    </tr>
</tbody>

        </table>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
