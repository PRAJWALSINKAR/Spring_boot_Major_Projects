<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" lang="en">
<head>
    <meta charset="UTF-8">
    <title>Plan Selection</title>
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { background-color: #f8f9fa; font-family: 'Segoe UI', sans-serif; }
        .navbar { background-color: #343a40 !important; }
        .sidebar { width: 70px; background-color: #343a40; position: fixed; height: 100vh; }
        main { margin-left: 70px; padding: 20px; }
        .form-container { background: white; padding: 30px; border-radius: 8px; }
    </style>
</head>
<body>

<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark px-3">
    <div class="d-flex align-items-center">
        <a class="navbar-brand d-flex align-items-center" href="#">
            <img th:src="@{/images/myName-Logo.png}" alt="Logo" style="height:35px"/>
            <span class="ms-2">IES</span>
        </a>
    </div>
</nav>

<main>
<div class="form-container">
    <h4 class="mb-4">Step 1: Plan Selection</h4>

    <!-- Flash messages -->
    <div th:if="${errMsg}" class="alert alert-danger" th:text="${errMsg}"></div>
    <div th:if="${succMsg}" class="alert alert-success" th:text="${succMsg}"></div>

    <!-- Applied Plans Table -->
    <div class="mb-4">
        <h5>Plans Already Applied</h5>
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>Case Number</th>
                    <th>Plan Name</th>
                    <th>Status</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <tr th:each="ap : ${appliedPlans}">
                    <td th:text="${ap.caseNumber}"></td>
                    <td th:text="${ap.selectedPlan}"></td>
                    <td><span class="badge bg-success">Applied</span></td>
                    <td>
                        <a th:href="@{|/citizen-plan/delete/${ap.id}/${ap.caseNumber}|}"
                           class="btn btn-sm btn-danger">Delete</a>
                    </td>
                </tr>
                <tr th:if="${appliedPlans.size() == 0}">
                    <td colspan="4" class="text-center text-muted">No plans applied yet.</td>
                </tr>
            </tbody>
        </table>
    </div>

    <!-- Plan Selection Form -->
    <form th:action="@{/citizen-plan/apply}" method="post">
        <input type="hidden" name="caseNumber" th:value="${caseNumber}"/>

        <div class="mb-3">
            <label class="form-label">Case Number</label>
            <input type="text" class="form-control" th:value="${caseNumber}" readonly>
        </div>

        <div class="mb-3">
            <label class="form-label">Select Plan</label>
            <select name="planName" class="form-select" required>
                <option value="" disabled selected>-- Select a Plan --</option>
                <option th:each="plan : ${plans}"
                        th:value="${plan.planName}"
                        th:text="|${plan.planName} (${plan.planCategory})|"></option>
            </select>
        </div>

        <div class="text-end">
            <button type="submit" class="btn btn-primary">Next →</button>
        </div>
    </form>
</div>
</main>

</body>
</html>
