<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Edit Caseworker</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"/>
</head>
<body>
<div class="container mt-4">
    <h2>Edit Caseworker</h2>

    <form th:action="@{/caseworker/update}" th:object="${caseworker}" method="post">
        <input type="hidden" th:field="*{id}"/>

        <div class="mb-3">
            <label>Name:</label>
            <input type="text" th:field="*{name}" class="form-control" required/>
        </div>

        <div class="mb-3">
            <label>Email:</label>
            <input type="email" th:field="*{email}" class="form-control" required readonly/>
        </div>

        <div class="mb-3">
            <label>Phone Number:</label>
            <input type="text" th:field="*{phoneNumber}" class="form-control" required/>
        </div>

        <div class="mb-3">
            <label>Gender:</label>
            <select th:field="*{gender}" class="form-select" required>
                <option value="">Select</option>
                <option value="Male" th:selected="*{gender} == 'Male'">Male</option>
                <option value="Female" th:selected="*{gender} == 'Female'">Female</option>
            </select>
        </div>

        <div class="mb-3">
            <label>DOB:</label>
            <form th:action="@{/caseworker/update}" th:object="${caseworker}" method="post">

        </div>

        <div class="mb-3">
            <label>SSN:</label>
            <input type="text" th:field="*{ssn}" class="form-control" required/>
        </div>

        <div class="mb-3">
            <label>Account Status:</label>
            <input type="text" th:field="*{accStatus}" class="form-control" readonly/>
        </div>

        <button type="submit" class="btn btn-success">Update</button>
        <a href="/caseworker/view" class="btn btn-secondary">Cancel</a>
    </form>
</div>
</body>
</html>
