<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Summary - Data Collection</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', sans-serif;
        }
        .navbar {
            background-color: #343a40 !important;
        }
        .navbar-brand img {
            height: 40px;
            width: 40px;
        }
        .summary-wrapper {
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
            padding: 32px;
            margin: 40px auto;
            max-width: 900px;
        }
        h4 {
            margin-bottom: 20px;
        }
        .section {
            margin-bottom: 30px;
        }
        .section-header {
            background-color: #f1f1f1;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 15px;
            font-weight: 600;
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark px-3 py-2">
    <div class="d-flex align-items-center">
        <button onclick="history.back()" class="btn btn-outline-light me-3" title="Go Back">&#8592;</button>
        <a class="navbar-brand d-flex align-items-center" href="#">
            <img th:src="@{/images/myName-Logo.png}" alt="Logo" class="me-2" />
            <span>IES</span>
        </a>
    </div>
    <div class="ms-auto d-flex gap-2">
        <a href="/dashboard" class="btn btn-light btn-sm">Dashboard</a>
        <a href="/logout" class="btn btn-danger btn-sm">Logout</a>
    </div>
</nav>

<div class="summary-wrapper">
    <h4>Application Summary</h4>

    <!-- --- Basic Citizen Info --- -->
    <div class="section">
        <div class="section-header">Citizen Basic Info</div>
        <p><strong>Full Name:</strong> <span th:text="${citizenEntity.fullName} ?: 'N/A'"></span></p>
        <p><strong>Email:</strong> <span th:text="${citizenEntity.email} ?: 'N/A'"></span></p>
        <p><strong>Mobile Number:</strong> <span th:text="${citizenEntity.mobileNumber} ?: 'N/A'"></span></p>
        <p><strong>Case Number:</strong> <span th:text="${citizenInfo.caseNumber}"></span></p>
    </div>

    <!-- --- Selected Plan --- -->
    <div class="section">
        <div class="section-header">Plan Selection</div>
        <p><strong>Plan:</strong> <span th:text="${citizenInfo.selectedPlan} ?: 'N/A'"></span></p>
    </div>

    <!-- --- Income Details --- -->
    <div class="section">
        <div class="section-header">Income Details</div>
        <p><strong>Monthly Salary:</strong> <span th:text="${citizenInfo.monthlySalary} ?: 'N/A'"></span></p>
        <p><strong>Rent Income:</strong> <span th:text="${citizenInfo.rentIncome} ?: 'N/A'"></span></p>
        <p><strong>Property Income:</strong> <span th:text="${citizenInfo.propertyIncome} ?: 'N/A'"></span></p>
    </div>

    <!-- --- Education Details --- -->
    <div class="section">
        <div class="section-header">Education Details</div>
        <p><strong>Highest Education:</strong> <span th:text="${citizenInfo.highestEducation} ?: 'N/A'"></span></p>
        <p><strong>Graduation Year:</strong> <span th:text="${citizenInfo.graduationYear} ?: 'N/A'"></span></p>
        <p><strong>University / School:</strong> <span th:text="${citizenInfo.universityOrSchool} ?: 'N/A'"></span></p>
    </div>

    <!-- --- Kids Details --- -->
    <div class="section">
        <div class="section-header">Kids Details</div>
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Kid Name</th>
                    <th>Age</th>
                    <th>SSN</th>
                </tr>
            </thead>
            <tbody>
                <tr th:each="kid : ${kids}">
                    <td th:text="${kid.kidName}"></td>
                    <td th:text="${kid.kidAge}"></td>
                    <td th:text="${kid.kidSsn}"></td>
                </tr>
                <tr th:if="${#lists.isEmpty(kids)}">
                    <td colspan="3" class="text-center">No kids data available</td>
                </tr>
            </tbody>
        </table>
    </div>

    <!-- --- Edit and Confirm Buttons --- -->
    <div class="d-flex justify-content-between">
        <a th:href="@{'/dc/start/' + ${citizenInfo.caseNumber}}" class="btn btn-warning">Edit</a>

        <form th:action="@{/dc/summary/confirm}" method="post">
            <input type="hidden" th:name="caseNumber" th:value="${citizenInfo.caseNumber}" />
            <button type="submit" class="btn btn-success">Confirm</button>
        </form>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
