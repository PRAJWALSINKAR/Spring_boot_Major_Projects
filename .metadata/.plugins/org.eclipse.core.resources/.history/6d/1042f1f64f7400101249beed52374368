<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Kids Details</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <style>
        body { background-color: #f8f9fa; font-family: 'Segoe UI', sans-serif; }
        .navbar { background-color: #343a40 !important; }
        .navbar-brand img { height: 40px; width: 40px; }
        .form-container { background: #fff; border-radius: 10px; box-shadow: 0 2px 8px rgba(0,0,0,0.04); padding: 32px; margin: 40px auto; max-width: 900px; }
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark px-3 py-2">
    <div class="d-flex align-items-center">
        <button onclick="history.back()" class="btn btn-outline-light me-3">&#8592;</button>
        <a class="navbar-brand d-flex align-items-center" href="#"><img th:src="@{/images/myName-Logo.png}" alt="Logo" class="me-2"/><span>IES</span></a>
    </div>
    <div class="ms-auto d-flex gap-2">
        <a href="/dashboard" class="btn btn-light btn-sm">Dashboard</a>
        <a href="/logout" class="btn btn-danger btn-sm">Logout</a>
    </div>
</nav>
<div class="form-container">
    <h4 class="mb-4">Step 4: Kids Details</h4>
    <form th:action="@{/dc/kids/save}" method="post">
        <input type="hidden" name="caseNumber" th:value="${caseNumber}" />
        <table class="table">
            <thead>
                <tr>
                    <th>Kid Name</th>
                    <th>Kid Age</th>
                    <th>Kid SSN</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody id="kidsTableBody">
                <tr th:each="kid, stat : ${kids}">
                    <td><input type="text" name="kids[__${stat.index}__].kidName" th:value="${kid.kidName}" class="form-control" required /></td>
                    <td><input type="number" name="kids[__${stat.index}__].kidAge" th:value="${kid.kidAge}" class="form-control" required /></td>
                    <td><input type="text" name="kids[__${stat.index}__].kidSsn" th:value="${kid.kidSsn}" class="form-control" required /></td>
                    <td>
                        <button type="button" class="btn btn-danger" onclick="removeKidRow(this)">Delete</button>
                    </td>
                </tr>
            </tbody>
        </table>
        <button type="button" id="addKidBtn" class="btn btn-success" onclick="addKidRow()">Add Kid</button>
        <div class="text-end mt-3">
            <button type="submit" class="btn btn-primary">Next →</button>
        </div>
    </form>
</div>
<script>
    function addKidRow() {
        var tbody = document.getElementById('kidsTableBody');
        var index = tbody.rows.length;
        var newRow = tbody.rows[0].cloneNode(true);
        // Clear values
        Array.from(newRow.querySelectorAll('input')).forEach(inp => inp.value = '');
        // Update name attributes (so Spring binds as List)
        Array.from(newRow.querySelectorAll('input')).forEach(inp => {
            inp.name = inp.name.replace(/\d+/, index);
        });
        tbody.appendChild(newRow);
    }
    function removeKidRow(btn) {
        var row = btn.closest('tr');
        var tbody = row.parentElement;
        if (tbody.rows.length > 1) { tbody.removeChild(row); }
    }
</script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
