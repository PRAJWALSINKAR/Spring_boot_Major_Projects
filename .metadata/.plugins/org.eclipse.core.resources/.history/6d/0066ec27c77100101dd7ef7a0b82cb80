<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>IES Login</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f5f6fa;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .login-container {
            background: white;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0px 5px 30px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
        }

        .login-logo {
            width: 150px;
            display: block;
            margin: 0 auto 20px auto;
        }

        .login-title {
            text-align: center;
            margin-bottom: 25px;
        }

        .btn-login {
            background-color: #6c63ff;
            color: white;
        }

        .forgot-password {
            text-align: center;
            margin-top: 10px;
        }

        input::placeholder {
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <!-- Logo with correct path and file name -->
        <img th:src="@{/images/myName-Logo.png}" src="/images/myName-Logo.png" alt="Logo" class="login-logo">

        <!-- Optional: app title -->
        <h2 class="login-title">Sign In</h2>
        <form th:action="@{/login}" method="post">
           <input type="text" name="username" placeholder="Enter Email Id" required>
           <input type="password" name="password" placeholder="Enter Password" required>
            <button type="submit" class="btn btn-login btn-block">Sign In</button>
        </form>
        <div class="forgot-password">
            <a th:href="@{/forgot-password}">Forgot Password?</a>
        </div>
    </div>
</body>
</html>
