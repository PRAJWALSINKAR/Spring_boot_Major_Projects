<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Create Application</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-3">
    <!-- Back Button -->
    <button onclick="history.back()" class="btn btn-outline-light me-2">&#8592;</button>

    <!-- Logo + IES -->
    <a class="navbar-brand d-flex align-items-center" href="#">
        <img th:src="@{/images/myName-Logo.png}" alt="Logo" width="40" height="40" class="me-2"/>
        <span>IES</span>
    </a>

    <!-- Right Side Buttons -->
    <div class="ms-auto d-flex gap-2">
        <a href="/plan/view" class="btn btn-light btn-sm">View Plans</a>
        <a href="/dashboard" class="btn btn-light btn-sm">Dashboard</a>
        <a href="/logout" class="btn btn-danger btn-sm">Logout</a>
    </div>
</nav>

<!-- Form -->
<div class="container mt-4">
    <h4 class="mb-4">Create Application</h4>
    <form th:action="@{/application/create}" method="post" th:object="${citizen}">
        <div class="row mb-3">
            <div class="col-md-6">
                <label>Full Name</label>
                <input type="text" class="form-control" th:field="*{fullName}" required/>
            </div>
            <div class="col-md-6">
                <label>Email ID</label>
                <input type="email" class="form-control" th:field="*{email}" required/>
            </div>
        </div>
        <div class="row mb-3">
            <div class="col-md-6">
                <label>Mobile Number</label>
                <input type="text" class="form-control" th:field="*{mobile}" required/>
            </div>
            <div class="col-md-6">
                <label>Gender</label><br/>
                <input type="radio" th:field="*{gender}" value="Male"/> Male
                <input type="radio" th:field="*{gender}" value="Female"/> Female
            </div>
        </div>
        <div class="row mb-3">
            <div class="col-md-6">
                <label>DOB</label>
                <input type="date" class="form-control" th:field="*{dob}" required/>
            </div>
            <div class="col-md-6">
                <label>SSN</label>
                <input type="text" class="form-control" th:field="*{ssn}" required/>
            </div>
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
</div>
</body>
</html>
