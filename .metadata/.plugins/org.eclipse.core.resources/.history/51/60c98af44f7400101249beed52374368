<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" lang="en">
<head>
    <meta charset="UTF-8">
    <title>Income Details</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <style>
        body { background-color: #f8f9fa; font-family: 'Segoe UI', sans-serif; }
        .navbar { background-color: #343a40 !important; }
        .navbar-brand img { height: 40px; width: 40px; }
        .form-container {
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.04);
            padding: 32px;
            margin: 40px auto;
            max-width: 800px;
        }
    </style>
</head>
<body>

<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark px-3 py-2">
    <div class="d-flex align-items-center">
        <button onclick="history.back()" class="btn btn-outline-light me-3" title="Go Back">&#8592;</button>
        <a class="navbar-brand d-flex align-items-center" href="#">
            <img th:src="@{/images/myName-Logo.png}" alt="Logo" class="me-2"/>
            <span>IES</span>
        </a>
    </div>
    <div class="ms-auto d-flex gap-2">
        <a href="/dashboard" class="btn btn-light btn-sm">Dashboard</a>
        <a href="/logout" class="btn btn-danger btn-sm">Logout</a>
    </div>
</nav>

<!-- Form -->
<div class="form-container">
    <h4 class="mb-4">Step 2: Income Details</h4>

    <form th:action="@{/dc/income/save}" method="post" th:object="${citizenInfo}">
        <div class="mb-3">
            <label class="form-label">Case Number</label>
            <input type="text" th:field="*{caseNumber}" class="form-control" readonly>
        </div>

        <div class="mb-3">
            <label class="form-label">Monthly Salary</label>
            <input type="number" th:field="*{monthlySalary}" class="form-control" step="0.01" min="0" required>
        </div>

        <div class="mb-3">
            <label class="form-label">Rent Income</label>
            <input type="number" th:field="*{rentIncome}" class="form-control" step="0.01" min="0">
        </div>

        <div class="mb-3">
            <label class="form-label">Property Income</label>
            <input type="number" th:field="*{propertyIncome}" class="form-control" step="0.01" min="0">
        </div>

        <div class="text-end">
            <button type="submit" class="btn btn-primary">Next →</button>
        </div>
    </form>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
